
before(noop);
beforeEach(noop);
after(noop);
afterEach(noop);

describe('suite', () => {
  before(noop);
  beforeEach(noop);
  after(noop);
  afterEach(noop);

  it('test 0', noop);
  it('test 1', noop);
});
